services:
  db:
    image: postgres:15-alpine
    env_file: ./call-monitor/.env
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $$POSTGRES_USER -d $$POSTGRES_DB"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s
    restart: unless-stopped
    networks: [zentcall-prod]

  app:
    image: ghcr.io/sazlain/call-monitor:main
    platform: linux/amd64
    env_file: ./call-monitor/.env
    environment:
      - DB_HOST=db
      - APP_BASE_URL=https://app.zentcall.com
      - SERVER_BASE_URL=https://api.zentcall.com
    ports:
      - "8080:8080"
    volumes:
      - uploads_data:/app/uploads
    depends_on:
      db:
        condition: service_healthy
    restart: unless-stopped
    networks: [zentcall-prod]

  frontend:
    image: ghcr.io/sazlain/voxio:main
    platform: linux/amd64
    env_file: ./voxio/.env
    ports:
      - "3000:80"
    restart: unless-stopped
    networks: [zentcall-prod]

  wa-bridge:
    image: ghcr.io/sazlain/zentcall-wa-bridge:main
    platform: linux/amd64
    environment:
      - BACKEND_URL=http://app:8080
      - BRIDGE_SECRET=${WA_BRIDGE_SECRET:-zentcall-bridge-secret}
      - PORT=3002
      - SESSIONS_DIR=/sessions
    volumes:
      - wa_sessions:/sessions
    depends_on:
      app:
        condition: service_started
    restart: unless-stopped
    networks: [zentcall-prod]
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:3002/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3

volumes:
  postgres_data:
  uploads_data:
  wa_sessions:

networks:
  zentcall-prod:
    driver: bridge
